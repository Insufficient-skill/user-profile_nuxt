{
  "name": "user-profile_nuxt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "mock": "json-server -w db.json -p 4200",
    "dev:mock": "concurrently \"npm run dev\" \"npm run mock\""
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2",
    "concurrently": "^5.2.0",
    "json-server": "^0.16.1"
  }
}
